// 1 inch = 96px
// 8.27 inch = 793.92 px
// 11.69 inch = 1122.24 px

// 2b274c

//  @material-tailwind/react
// outline: none; // In CSS

// npm i react-image-crop

//<input type="file" accept='image/*' onChange={(e)=>{setUserImg(URL.createObjectURL(e.target.files[0]))}} id="file" className='hidden'/>
//<label htmlFor="file" className='border rounded-lg px-3 py-1 bg-amber-300 hover:bg-amber-400 cursor-pointer'>Upload Photo</label>

// npx create-react-app localstorage-react-hook

// JSON.stringlify() : array/obj to string
// JSON.parse() : string to array/object...

 /*
   Always use USE Context for,
   - Notification
   - theam
   - Font Size etc....

   UseState
   - The second parameter is used to update the state.

   UseEffect
   - It allows to run the code after the component has been rendered.
  */
  

  // SAVE AS PDF
  // npm install html2canvas jspdf

  // We can't directly access the DOM elements in React Functional Components so we have to use the useRef() hook.
  // The work of id in other while selecting DOM element is done by the "ref" attribute.
  
  // npm install html2pdf.js